name: lab01
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install nbconvert

      - run: pip install inkex
      
      - run: sudo apt-get install pandoc
      - run: python -m nbconvert --to pdf pint_and_boiling_point.ipynb

